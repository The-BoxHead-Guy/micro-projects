# Custom name for the final executable
TARGET = ../bin/hello

# The source file(s) to compile
SRC = hello.c

all: $(TARGET)

$(TARGET): $(SRC)
	gcc $(SRC) -o $(TARGET) -lcs50

clean:
	rm -f $(TARGET)
