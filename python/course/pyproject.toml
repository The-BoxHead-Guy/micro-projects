[project]
name = "course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.34.2",
]

[tool.uv.workspace]
members = [
    "projects/habit_tracker",
    "projects/report_generator",
]
